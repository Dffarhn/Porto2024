---
import { CardProjectComponent } from "@/helper/landingpage/card";

const projectData = [
  {
    title: "IMUII",
    subtitle: "End-to-End Automation Platform",
    description:
      "IMUII is an automation platform I built from scratch as Founder & Technical Architect. It manages 45+ active projects with an 82% deployment success rate.",
    features: [
      "Automated CI/CD",
      "One-command project initialization",
      "Real-time monitoring",
      "GitHub & environment provisioning"
    ],
    techStack: ["Golang", "Docker", "PostgreSQL", "Redis"],
    impact: "Used across dozens of active projects • ~19% lower CPU usage in production",
    img: "/project/LancarCom.png", // Placeholder  replace with IMUII image
  },
  {
    title: "HalalMate",
    subtitle: "AI-Powered Mobile App",
    description:
      "HalalMate is an AI-powered mobile app that helps Muslim travelers verify halal food automatically. I built the backend services and AI pipeline using OCR and OpenAI.",
    features: [
      "AI-powered halal verification",
      "OCR for food label scanning",
      "Google Maps integration"
    ],
    techStack: ["Golang", "Flutter", "OpenAI API", "OCR", "Google Maps API"],
    impact: "93% classification accuracy • 100% recall",
    img: "/project/Nasho.png", // Placeholder  replace with HalalMate image
  },
  {
    title: "KeepUp",
    subtitle: "Mental Health Research System",
    description:
      "Backend system for mental health research at Universitas Islam Indonesia. It handles secure authentication, role-based access control, and automated report generation.",
    features: [
      "Secure authentication & RBAC",
      "Automated report generation (PDF & Excel)",
      "AI-powered report interpretation"
    ],
    techStack: ["NestJS", "PostgreSQL", "Redis", "OpenAI API"],
    impact: "A secure, automated research pipeline for mental health studies",
    img: "/project/TEDXUII.png", // Placeholder  replace with KeepUp image
  },
];
---

<div class="projects-section">
  <div class="section-background">
    <div class="gradient-blob blob-1"></div>
    <div class="gradient-blob blob-2"></div>
  </div>
  <div class="flex flex-col laptop:flex-row laptop:flex-wrap items-center my-24 text-white w-full px-6 relative z-10">
  <div class="mb-8 text-center w-full">
    <h3 class="text-3xl laptop:text-5xl font-bold">Projects</h3>
    <p class="text-lg laptop:text-2xl text-gray-400">A selection of projects I've shipped</p>
  </div>
  <div class="flex flex-col laptop:flex-row gap-8 justify-center w-full">
    {projectData.map((project, index) => (
      <CardProjectComponent 
        title={project.title} 
        subtitle={project.subtitle}
        description={project.description} 
        features={project.features}
        techStack={project.techStack}
        impact={project.impact}
        img={project.img} 
      />
    ))}
  </div>
</div>

<style>
  .projects-section {
    position: relative;
    overflow: hidden;
    width: 100%;
  }

  .section-background {
    position: absolute;
    inset: 0;
    z-index: 0;
    overflow: hidden;
    pointer-events: none;
  }

  .gradient-blob {
    position: absolute;
    border-radius: 50%;
    filter: blur(60px);
    opacity: 0.10;
  }

  .blob-1 {
    width: 600px;
    height: 600px;
    background: radial-gradient(circle, rgba(var(--primary), 0.35), rgba(var(--primary), 0));
    top: -200px;
    left: -150px;
    animation: float-projects-1 14s ease-in-out infinite alternate;
  }

  .blob-2 {
    width: 550px;
    height: 550px;
    background: radial-gradient(circle, rgba(var(--accent), 0.22), rgba(var(--accent), 0));
    bottom: -250px;
    right: -100px;
    animation: float-projects-2 16s ease-in-out infinite alternate-reverse;
  }

  @keyframes float-projects-1 {
    0% { transform: translate(0, 0) rotate(0deg); }
    100% { transform: translate(50px, 60px) rotate(12deg); }
  }

  @keyframes float-projects-2 {
    0% { transform: translate(0, 0) rotate(0deg); }
    100% { transform: translate(-60px, -40px) rotate(-15deg); }
  }
</style>
